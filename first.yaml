 - name: my first playbook
   hosts: all
   become: true
   tasks: 
     - name: create a directory
       file:
         name: dir1 
         path: /tmp/
         state: directory
         register: vikrant

       - name: check the debug
         debug:
           var: vikrant      

